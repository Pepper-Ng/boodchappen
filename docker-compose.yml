services:
  backend:
    build: ./backend
    ports: ["8000:8000"]
    environment:
      - DATABASE_URL=sqlite:///./app.db
      - SECRET_KEY=change-me
  frontend:
    build: ./frontend
    ports: ["5173:5173"]
    depends_on: [backend]
